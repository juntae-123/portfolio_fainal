import { RivewinterInterceptor } from './rivewinter.interceptor';

describe('RivewinterInterceptor', () => {
  it('should be defined', () => {
    expect(new RivewinterInterceptor()).toBeDefined();
  });
});
